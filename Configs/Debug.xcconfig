SWIFT_ACTIVE_COMPILATION_CONDITIONS = $(inherited) DEBUG
SWIFT_OPTIMIZATION_LEVEL = -Onone

// Machine-local settings (team id). Optional: absent in a fresh clone and in CI.
#include? "Local.xcconfig"
